apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oimis-fe.fullname" . }}-test-connection"
  labels:
    {{- include "oimis-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oimis-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
