apiVersion: v1
kind: Service
metadata:
  name: {{ include "oimis-fe.fullname" . }}
  labels:
    {{- include "oimis-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oimis-fe.selectorLabels" . | nindent 4 }}
